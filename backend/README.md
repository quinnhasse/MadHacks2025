Required version:
  Python 3.13.7

Initialize environment:
  python -m venv .venv

Start environment:
  source .venv/bin/activate


How to capture pip packages:
  pip freeze > requirements.txt

How to install pip packages:
  pip install -r requirements.txt
